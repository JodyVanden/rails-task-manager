<h1>Index</h1>

<ol>
  <li> <% @tasks.each do |t| %>
    <h2> <%= link_to t.name, task_path(t) %> </h2>
    <p> <%= t.description %> </p>
    <% end %>
  </li>

</ol>

<h2> <%= link_to 'show all tasks', tasks_path %> </h2>
